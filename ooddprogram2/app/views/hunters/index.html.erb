<p id="notice"><%= notice %></p>
<h1>Welcome <%= current_user.name %></h1>

<table>
  <thead>
    <tr>
<!--      <th>User</th>-->
      <th>User Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
<% @hunters.each do |hunter| %>
      <tr>
<!--        <td><%#= hunter.user_id %></td>-->
        <td><%= hunter.user.name %></td>
        <td><%= link_to 'Show', hunter %></td>
        <td><%= link_to 'Edit', edit_hunter_path(hunter) %></td>
        <% if hunter.user.name != "Admin" %>
          <td><%= link_to 'Destroy', hunter, method: :delete, data: { confirm: 'Are you sure?' } %></td>

      <% end %>
<!--        <td><%#= link_to 'Destroy', hunter, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
<% end %>
  </tbody>
</table>
  <%#= link_to 'New Hunter', new_hunter_path %>
<br>

<%if logged_in_admin? %>
  <%= link_to "Back", current_user%>

<%else %>
  <%= link_to "Back", home_hunter_path(@current_user.hunter.id)%>

<%end %>

<br><% if logged_in? %>
  <%= link_to "Log out", logout_path, method: :delete %>
<% end %>
